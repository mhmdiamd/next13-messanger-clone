'use client'

import { Toaster } from "react-hot-toast"
import React from 'react'

type Props = {}

const ToasterContex = (props: Props) => {
  return (
    <Toaster />
  )
}

export default ToasterContex